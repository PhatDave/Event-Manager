package com.hackathlon.hackathlon.mapper.registrationMappers;

import org.mapstruct.*;

@Mapper(builder = @Builder(disableBuilder=true))
public interface NameMapper {
//    TODO: ?
//    Name toEntity(NameRequestDto dto);
}
